meta:
  environment: node
  toolchain:
    name: browser
    version: "18"
build:
  path: frontend/smart-table-customer/
  additionalCommands: npm install && npm run build
  artifacts:
    build/*: /
run: 
  command: npm start
