meta:
  environment: node
  toolchain:
    name: browser
    version: "18"
build:
  path: frontend/smart-table-customer/
  build: build
  root: /
  additionalCommands: null
  artifacts:
    build/*: /
run: {}
